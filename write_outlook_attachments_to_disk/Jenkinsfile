pipeline {
    agent { label 'window-agent-02' }

    stages {
        stage('hello') {
            steps {
                script {
                pwsh.exe "ls"
                }
            }
        }
    }
}
